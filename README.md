npm install - set up packages 
gulp watch - prepare dev version, open dev server
gulp fast-build - compile and pack files (without images) for final checking
gulp build - prepare all files for release